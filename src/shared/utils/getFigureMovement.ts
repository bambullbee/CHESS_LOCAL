import { FigureType } from "../types/chessTypes";

const getFigureMovement = (
  figure: FigureType,
  figureId: number,
  stepId: number,
  attacks: number[]
) => {
  switch (figure) {
    case "pawn":

    default:
  }
};

export default getFigureMovement;
